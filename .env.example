# Copy to .env for local dev. Not needed when deployed as a Databricks App
# if its auto-injected credentials work (see backend/main.py's fallback
# path) -- DATABRICKS_HTTP_PATH is still required either way.

# SQL warehouse's "Connection details" tab in the Databricks UI:
DATABRICKS_SERVER_HOSTNAME=
DATABRICKS_HTTP_PATH=

# Personal access token -- simplest path for local dev. Leave unset to use
# the Databricks SDK's default credential chain (e.g. a configured CLI
# profile) instead.
DATABRICKS_TOKEN=

# Model Serving endpoint name (Serving UI) for the AI negotiation-narrative
# feature -- optional; that feature 503s with a clear "not configured"
# message if unset, everything else works without it. Whichever identity
# DATABRICKS_TOKEN (or the SDK default credential chain) resolves to also
# needs "Can Query" granted on this endpoint. databricks-claude-haiku-4-5
# is a Databricks-hosted foundation model endpoint on this workspace,
# confirmed working via a direct notebook test -- see databricks/FINDINGS.md.
DATABRICKS_LLM_ENDPOINT=databricks-claude-haiku-4-5
