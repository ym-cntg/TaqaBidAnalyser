# Databricks Apps deployment manifest.
#
# NOT YET VERIFIED against a real deployment -- this is a best-effort
# manifest based on documented Databricks Apps conventions, not something
# that's been tested end-to-end. The whole point of this branch's first
# step is to actually try deploying and fix whatever this gets wrong
# (exact schema, env var names, whether a SQL warehouse resource needs to
# be declared here vs. attached via the Apps UI, etc.) -- treat this file
# as a starting point, not a confirmed-working config.
command: ["bash", "start.sh"]

env:
  - name: DATABRICKS_HTTP_PATH
    value: ""  # fill in from the target SQL warehouse's "Connection details" tab
