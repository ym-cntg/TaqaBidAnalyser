[project]
name = "taqa-maximo-data-analysis"
version = "0.1.0"
description = "Exploration of real Maximo procurement data (RFQ/vendor/quotation tables) via Databricks Unity Catalog"
readme = "README.md"
requires-python = ">=3.11.11"
dependencies = [
    "pandas>=2.0.0",
    "python-dotenv>=1.2.2",
]
